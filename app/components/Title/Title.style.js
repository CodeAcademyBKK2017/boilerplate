import {
  StyleSheet
} from 'react-native';
  
const titleStyles = StyleSheet.create({
  inputBox: {
    minWidth: '100%',
    borderColor: 'gray',
    borderWidth: 1,
    marginTop: 20,
    backgroundColor: 'white',
    padding: 10
  },
  fontTitle: {
    fontWeight: 'bold'
  }
});
  
export default titleStyles;