import {
  StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%'
  },
  title: {
    paddingVertical: 10,
    fontStyle: 'italic'
  },
  textInput: {
    width: '100%',
    minHeight: 100,
    borderColor: 'gray',
    borderWidth: 1,
    padding: 5
  }
});

export default styles;
