// import Notes from '../notes.reducer';
// import {ADD_NOTE, DELETE_NOTE, GET_NOTE} from '../../actions/index.actions';

describe('Notes Reducer Testing', () => {
  xit('renders correctly ADD_NOTE', () => {

    // const result = Notes([], {type: SHOW_LOADER});
    // expect(result).toEqual({visible: true});
  });
  xit('renders correctly DELETE_NOTE', () => {
    
  });
  xit('renders correctly GET_NOTE', () => {
    
  });
});
